<Page
    x:Class="encedit.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:encedit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Name="New" x:Uid="New" Icon="Add" Label="New"/>
                <AppBarButton x:Name="Save" x:Uid="Save" Icon="Save" Label="Save"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="Headding" x:Uid="Headding" Text="Headding" Margin="10,10,0,0"  FontSize="26.667" HorizontalAlignment="Left" VerticalAlignment="Top" />
            <PasswordBox x:Name="Password" FontSize="14.667" Margin="10,60,10,0" VerticalAlignment="Top"  />
            <TextBox x:Name="ContentText"  AcceptsReturn="True" Margin="10,100,10,10" />
    </Grid>
</Page>
